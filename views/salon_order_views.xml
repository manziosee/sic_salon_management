<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="salon_order_view_form" model="ir.ui.view">
        <field name="name">salon.order.view.form</field>
        <field name="model">salon.order</field>
        <field name="inherit_id" ref="salon_management.salon_order_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="replace">
                <header>
                    <button name="action_confirm" string="Confirm" type="object"
                            class="oe_highlight" attrs="{'invisible': [('state', '!=', 'draft')]}"/>
                    <button name="action_invoice" string="Invoice" type="object"
                            class="oe_highlight" attrs="{'invisible': [('state', '!=', 'confirmed')]}"/>
                    <button name="action_done" string="Mark Done" type="object"
                            class="oe_highlight" attrs="{'invisible': [('state', '!=', 'invoiced')]}"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,invoiced,done"
                           statusbar_colors='{"draft":"blue", "confirmed":"orange", "invoiced":"green", "done":"purple"}'
                           options="{'clickable': '1'}"/>
                </header>
            </xpath>
        </field>
    </record>
</odoo>
